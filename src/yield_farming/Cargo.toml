[package]
name = "soroban-yield-farming"
version = "0.0.0"
edition = "2021"

[dependencies]
soroban-sdk = "26.0.1"

[dev-dependencies]
soroban-sdk = { version = "26.0.1", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
